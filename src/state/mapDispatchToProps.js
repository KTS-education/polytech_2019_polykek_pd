import { setToken, setProfile, setUserId } from './profile';
import { setWishlistIds } from './wishlistItems';

const mapDispatchToProps = {
  setToken,
  setProfile,
  setUserId,
  setWishlistIds,
};

export default mapDispatchToProps;
